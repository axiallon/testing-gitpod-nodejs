class OddService {

    /**
     * Given an integer array, return true if there are three consecutive
     * odd numbers in the array. Otherwise, return false.
     * 
     * Constraints:
     *   Numbers array will always have at least 3 numbers
     * 
     * @param {Array<Number>} nums The array of numbers to check.
     * @return {boolean} True if array contains 3 consecutive odd numbers, otherwise false.
     */
    static hasThreeConsecutiveOdds(nums) {
        throw new Error("Not implemented yet");
    }

}

module.exports = OddService;